<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.qiaomu.test.mapper.two.UserTwoMapper" >
    <update id="update" parameterType="com.qiaomu.test.domain.User">
        <!--update user
        <set>
            <if test="name != null">
                name= #{name},
            </if>
            <if test="age != null">
                age= #{age},
            </if>
        </set>
        where id= #{id}-->
        update user set name=#{name},age=#{age} where id = #{id}
    </update>
</mapper>